package fi.margokomarova.calculator.service;

public class ResultWriterService {
    public static void printResult(int num1, int num2, int result, String name) {
        System.out.println(num1 + ", " + num2 + ", " + result + ", " + name);
    }
}
